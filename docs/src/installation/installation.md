# Installation with Julia

You can install AutomationLabs with:

```julia
julia> import Pkg
julia> Pkg.add("AutomationLabs")
```
Then, you can use the package with:

```julia
julia> using AutomationLabs
```
